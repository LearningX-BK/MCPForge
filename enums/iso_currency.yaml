# ISO 4217 currency codes referenced by `enumRef: iso_currency` (02 §2.2, §5.3).
# An `enumRef` list is never inlined into a generated schema — it is fetched via
# lookup — so this file may grow without costing an agent tokens.
id: iso_currency
values:
  [
    AUD,
    CAD,
    CHF,
    CNY,
    DKK,
    EUR,
    GBP,
    HKD,
    INR,
    JPY,
    NOK,
    NZD,
    PLN,
    SEK,
    SGD,
    USD,
    ZAR,
  ]
