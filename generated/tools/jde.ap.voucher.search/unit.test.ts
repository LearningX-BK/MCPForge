// GENERATED BY forge codegen FROM manifests/jde/fin/ap/voucher.search.tool.yaml
// manifest-sha256: c93b308dbdde2aa1c58f24352b4b317a97eb00951f57ad9cb10ce6ea08b8c87d  codegen-version: 0.0.0   DO NOT EDIT

import { describe, expect, it } from 'vitest';
import { evaluateGuardrails, validateArgs, type Args } from './handler.generated.js';
import { ERROR_TAXONOMY } from '@mcpforge/shared/errors';

const VALID_ARGS = {
  supplier_number: 'test-supplier_number',
  company: 'test-company',
  pay_status: 'test-pay_status',
  document_type: 'test-document_type',
  gl_date_from: '2024-01-15',
  gl_date_to: '2024-01-15',
} as unknown as Args;

describe('jde.ap.voucher.search — schema boundary (generated, W0-B6)', () => {
  it('accepts a fully valid argument set', () => {
    expect(validateArgs(VALID_ARGS)).toBe(true);
  });

  it('rejects a call missing required field "supplier_number"', () => {
    const { supplier_number: _omit, ...rest } = VALID_ARGS as unknown as Record<string, unknown>;
    expect(validateArgs(rest)).toBe(false);
  });

  it('rejects the wrong type for "supplier_number" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, supplier_number: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('rejects the wrong type for "company" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, company: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('rejects the wrong type for "pay_status" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, pay_status: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('rejects the wrong type for "document_type" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, document_type: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('rejects the wrong type for "gl_date_from" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, gl_date_from: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('rejects the wrong type for "gl_date_to" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, gl_date_to: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('every declared error path (INPUT_INVALID, TARGET_ERROR) carries a non-empty, agent-actionable next', () => {
    const codes = ['INPUT_INVALID', 'TARGET_ERROR'] as const;
    for (const code of codes) {
      const spec = ERROR_TAXONOMY[code];
      expect(spec.next).toBeTruthy();
      expect(spec.next.trim().length).toBeGreaterThan(0);
      expect(spec.next).not.toMatch(/^try again\.?$/i);
    }
  });
});
