// GENERATED BY forge codegen FROM manifests/jde/fin/ap/voucher.get.tool.yaml
// manifest-sha256: 2d3636f59be7cae0be8d769062c370bbe90821dd970cd31b7e538db7a8817d0b  codegen-version: 0.0.0   DO NOT EDIT

import { describe, expect, it } from 'vitest';
import { evaluateGuardrails, validateArgs, type Args } from './handler.generated.js';
import { ERROR_TAXONOMY } from '@mcpforge/shared/errors';

const VALID_ARGS = {
  document_number: 'test-document_number',
  document_type: 'test-document_type',
  document_company: 'test-document_company',
} as unknown as Args;

describe('jde.ap.voucher.get — schema boundary (generated, W0-B6)', () => {
  it('accepts a fully valid argument set', () => {
    expect(validateArgs(VALID_ARGS)).toBe(true);
  });

  it('rejects a call missing required field "document_number"', () => {
    const { document_number: _omit, ...rest } = VALID_ARGS as unknown as Record<string, unknown>;
    expect(validateArgs(rest)).toBe(false);
  });

  it('rejects a call missing required field "document_type"', () => {
    const { document_type: _omit, ...rest } = VALID_ARGS as unknown as Record<string, unknown>;
    expect(validateArgs(rest)).toBe(false);
  });

  it('rejects a call missing required field "document_company"', () => {
    const { document_company: _omit, ...rest } = VALID_ARGS as unknown as Record<string, unknown>;
    expect(validateArgs(rest)).toBe(false);
  });

  it('rejects the wrong type for "document_number" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, document_number: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('rejects the wrong type for "document_type" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, document_type: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('rejects the wrong type for "document_company" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, document_company: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('every declared error path (INPUT_INVALID, TARGET_ERROR) carries a non-empty, agent-actionable next', () => {
    const codes = ['INPUT_INVALID', 'TARGET_ERROR'] as const;
    for (const code of codes) {
      const spec = ERROR_TAXONOMY[code];
      expect(spec.next).toBeTruthy();
      expect(spec.next.trim().length).toBeGreaterThan(0);
      expect(spec.next).not.toMatch(/^try again\.?$/i);
    }
  });
});
