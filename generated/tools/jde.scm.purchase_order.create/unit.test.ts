// GENERATED BY forge codegen FROM manifests/jde/scm/po/purchase_order.create.tool.yaml
// manifest-sha256: 4ea44407b2037e115920f688b29de18113e0f8ab1bbb442d3addf2f80791f0de  codegen-version: 0.0.0   DO NOT EDIT

import { describe, expect, it } from 'vitest';
import { evaluateGuardrails, validateArgs, type Args } from './handler.generated.js';
import { ERROR_TAXONOMY } from '@mcpforge/shared/errors';

const VALID_ARGS = {
  supplier_number: '4242',
  company: '00100',
  business_unit: 'test-business_unit',
  item_number: 'test-item_number',
  quantity: 1.001,
  unit_cost: 1.01,
  currency: 'GBP',
  required_date: '2024-01-15',
} as unknown as Args;

describe('jde.scm.purchase_order.create — schema boundary (generated, W0-B6)', () => {
  it('accepts a fully valid argument set', () => {
    expect(validateArgs(VALID_ARGS)).toBe(true);
  });

  it('rejects a call missing required field "supplier_number"', () => {
    const { supplier_number: _omit, ...rest } = VALID_ARGS as unknown as Record<string, unknown>;
    expect(validateArgs(rest)).toBe(false);
  });

  it('rejects a call missing required field "company"', () => {
    const { company: _omit, ...rest } = VALID_ARGS as unknown as Record<string, unknown>;
    expect(validateArgs(rest)).toBe(false);
  });

  it('rejects a call missing required field "business_unit"', () => {
    const { business_unit: _omit, ...rest } = VALID_ARGS as unknown as Record<string, unknown>;
    expect(validateArgs(rest)).toBe(false);
  });

  it('rejects a call missing required field "item_number"', () => {
    const { item_number: _omit, ...rest } = VALID_ARGS as unknown as Record<string, unknown>;
    expect(validateArgs(rest)).toBe(false);
  });

  it('rejects a call missing required field "quantity"', () => {
    const { quantity: _omit, ...rest } = VALID_ARGS as unknown as Record<string, unknown>;
    expect(validateArgs(rest)).toBe(false);
  });

  it('rejects a call missing required field "unit_cost"', () => {
    const { unit_cost: _omit, ...rest } = VALID_ARGS as unknown as Record<string, unknown>;
    expect(validateArgs(rest)).toBe(false);
  });

  it('rejects a call missing required field "currency"', () => {
    const { currency: _omit, ...rest } = VALID_ARGS as unknown as Record<string, unknown>;
    expect(validateArgs(rest)).toBe(false);
  });

  it('rejects the wrong type for "supplier_number" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, supplier_number: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('rejects the wrong type for "company" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, company: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('rejects the wrong type for "business_unit" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, business_unit: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('rejects the wrong type for "item_number" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, item_number: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('rejects the wrong type for "quantity" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, quantity: 'not-a-number' };
    expect(validateArgs(bad)).toBe(false);
  });

  it('rejects the wrong type for "unit_cost" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, unit_cost: 'not-a-number' };
    expect(validateArgs(bad)).toBe(false);
  });

  it('rejects the wrong type for "currency" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, currency: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('rejects the wrong type for "required_date" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, required_date: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('accepts confirm omitted, confirm: null, and confirm as a string', () => {
    expect(validateArgs(VALID_ARGS)).toBe(true);
    expect(validateArgs({ ...VALID_ARGS, confirm: null })).toBe(true);
    expect(validateArgs({ ...VALID_ARGS, confirm: 'cnf_test' })).toBe(true);
  });

  it('the maxNumeric guardrail on "unit_cost" breaches above, not below, its 250000 threshold', () => {
    const overArgs = { ...VALID_ARGS, unit_cost: 250001 } as unknown as Args;
    const over = evaluateGuardrails(overArgs);
    expect(over.breached).toBe(true);

    const underArgs = { ...VALID_ARGS, unit_cost: 249999 } as unknown as Args;
    const under = evaluateGuardrails(underArgs);
    expect(under.breached).toBe(false);
  });

  it('every declared error path (INPUT_INVALID, TARGET_ERROR, POLICY_GUARDRAIL_BREACH, PLAN_ARGUMENT_MISMATCH) carries a non-empty, agent-actionable next', () => {
    const codes = [
      'INPUT_INVALID',
      'TARGET_ERROR',
      'POLICY_GUARDRAIL_BREACH',
      'PLAN_ARGUMENT_MISMATCH',
    ] as const;
    for (const code of codes) {
      const spec = ERROR_TAXONOMY[code];
      expect(spec.next).toBeTruthy();
      expect(spec.next.trim().length).toBeGreaterThan(0);
      expect(spec.next).not.toMatch(/^try again\.?$/i);
    }
  });
});
