{
  "$id": "https://mcpforge.ltm/schema/generated/tools/jde.scm.purchase_order.create/schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "//1": "GENERATED BY forge codegen FROM manifests/jde/scm/po/purchase_order.create.tool.yaml",
  "//2": "manifest-sha256: 4ea44407b2037e115920f688b29de18113e0f8ab1bbb442d3addf2f80791f0de  codegen-version: 0.0.0   DO NOT EDIT",
  "additionalProperties": false,
  "properties": {
    "business_unit": {
      "description": "Business unit the order is charged to.",
      "type": "string"
    },
    "company": {
      "description": "JDE company code.",
      "examples": ["00100"],
      "type": "string"
    },
    "confirm": {
      "description": "Omit or null to PLAN (no change is made). Pass the confirmToken returned by the plan to EXECUTE.",
      "type": ["string", "null"]
    },
    "currency": {
      "description": "ISO currency code, e.g. GBP.",
      "examples": ["GBP"],
      "type": "string"
    },
    "item_number": {
      "description": "Item number or a short line description.",
      "type": "string"
    },
    "quantity": {
      "description": "Quantity ordered in the item's unit.",
      "minimum": 0.001,
      "type": "number"
    },
    "required_date": {
      "description": "Date goods are required, YYYY-MM-DD.",
      "format": "date",
      "type": "string"
    },
    "supplier_number": {
      "description": "JDE address book number of the supplier.",
      "examples": ["4242"],
      "type": "string"
    },
    "unit_cost": {
      "description": "Cost per unit in the order currency.",
      "minimum": 0.01,
      "type": "number"
    }
  },
  "required": [
    "supplier_number",
    "company",
    "business_unit",
    "item_number",
    "quantity",
    "unit_cost",
    "currency"
  ],
  "title": "jde.scm.purchase_order.create input",
  "type": "object"
}
