// GENERATED BY forge codegen FROM manifests/jde/scm/po/purchase_order.get_receipt_status.tool.yaml
// manifest-sha256: 0fb1b0f0a6ef438a2557492d68b07049a2458f4d4dc0d434b63c8b4a15efc081  codegen-version: 0.0.0   DO NOT EDIT

import { describe, expect, it } from 'vitest';
import { evaluateGuardrails, validateArgs, type Args } from './handler.generated.js';
import { ERROR_TAXONOMY } from '@mcpforge/shared/errors';

const VALID_ARGS = {
  order_number: 'test-order_number',
  order_type: 'test-order_type',
  order_company: 'test-order_company',
} as unknown as Args;

describe('jde.scm.purchase_order.get_receipt_status — schema boundary (generated, W0-B6)', () => {
  it('accepts a fully valid argument set', () => {
    expect(validateArgs(VALID_ARGS)).toBe(true);
  });

  it('rejects a call missing required field "order_number"', () => {
    const { order_number: _omit, ...rest } = VALID_ARGS as unknown as Record<string, unknown>;
    expect(validateArgs(rest)).toBe(false);
  });

  it('rejects the wrong type for "order_number" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, order_number: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('rejects the wrong type for "order_type" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, order_type: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('rejects the wrong type for "order_company" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, order_company: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('every declared error path (INPUT_INVALID, TARGET_ERROR) carries a non-empty, agent-actionable next', () => {
    const codes = ['INPUT_INVALID', 'TARGET_ERROR'] as const;
    for (const code of codes) {
      const spec = ERROR_TAXONOMY[code];
      expect(spec.next).toBeTruthy();
      expect(spec.next.trim().length).toBeGreaterThan(0);
      expect(spec.next).not.toMatch(/^try again\.?$/i);
    }
  });
});
