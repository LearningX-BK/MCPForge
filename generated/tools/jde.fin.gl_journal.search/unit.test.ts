// GENERATED BY forge codegen FROM manifests/jde/fin/gl/gl_journal.search.tool.yaml
// manifest-sha256: 2b223a5515ea184f5000254e17cd26526f3c0bb67f24be070a174d03702533d2  codegen-version: 0.0.0   DO NOT EDIT

import { describe, expect, it } from 'vitest';
import { evaluateGuardrails, validateArgs, type Args } from './handler.generated.js';
import { ERROR_TAXONOMY } from '@mcpforge/shared/errors';

const VALID_ARGS = {
  company: 'test-company',
  batch_number: 'test-batch_number',
  document_type: 'test-document_type',
  account_number: 'test-account_number',
  date_from: '2024-01-15',
  date_to: '2024-01-15',
} as unknown as Args;

describe('jde.fin.gl_journal.search — schema boundary (generated, W0-B6)', () => {
  it('accepts a fully valid argument set', () => {
    expect(validateArgs(VALID_ARGS)).toBe(true);
  });

  it('rejects a call missing required field "company"', () => {
    const { company: _omit, ...rest } = VALID_ARGS as unknown as Record<string, unknown>;
    expect(validateArgs(rest)).toBe(false);
  });

  it('rejects the wrong type for "company" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, company: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('rejects the wrong type for "batch_number" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, batch_number: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('rejects the wrong type for "document_type" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, document_type: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('rejects the wrong type for "account_number" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, account_number: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('rejects the wrong type for "date_from" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, date_from: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('rejects the wrong type for "date_to" (type coercion is not permitted)', () => {
    const bad = { ...VALID_ARGS, date_to: 12345 };
    expect(validateArgs(bad)).toBe(false);
  });

  it('every declared error path (INPUT_INVALID, TARGET_ERROR) carries a non-empty, agent-actionable next', () => {
    const codes = ['INPUT_INVALID', 'TARGET_ERROR'] as const;
    for (const code of codes) {
      const spec = ERROR_TAXONOMY[code];
      expect(spec.next).toBeTruthy();
      expect(spec.next.trim().length).toBeGreaterThan(0);
      expect(spec.next).not.toMatch(/^try again\.?$/i);
    }
  });
});
